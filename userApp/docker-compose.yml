version: "3.8"

services:
  taskflow:
    build: .
    ports:
      - "3000:3000"
    command: node src/index.js

  student:
    build: .
    ports:
      - "3001:3001"
    command: node student/index.js